definition depositSig() returns uint32 = sig:deposit(uint256,address).selector;
definition depositWithTypeSig() returns uint32 = sig:deposit(uint256,address,ISilo.AssetType).selector;
definition withdrawSig() returns uint32 = sig:withdraw(uint256,address,address).selector;
definition withdrawWithTypeSig() returns uint32 = sig:withdraw(uint256,address,address,ISilo.AssetType).selector;
definition mintSig() returns uint32 = sig:mint(uint256,address).selector;
definition mintWithTypeSig() returns uint32 = sig:mint(uint256,address,ISilo.AssetType).selector;
definition accrueInterestSig() returns uint32 = sig:accrueInterest().selector;
definition transitionCollateralSig() returns uint32 = sig:transitionCollateral(uint256,address,ISilo.AssetType).selector;
definition initializeSig() returns uint32 = sig:initialize(address,address).selector;
definition withdrawFeesSig() returns uint32 = sig:withdrawFees().selector;
definition flashLoanSig() returns uint32 = sig:flashLoan(address,address,uint256,bytes).selector;
definition transferSig() returns uint32 = sig:transfer(address,uint256).selector;
definition transferFromSig() returns uint32 = sig:transferFrom(address,address,uint256).selector;
definition approveSig() returns uint32 = sig:approve(address,uint256).selector;
definition redeemSig() returns uint32 = sig:redeem(uint256,address,address).selector;
definition repaySig() returns uint32 = sig:repay(uint256,address).selector;
definition repaySharesSig() returns uint32 = sig:repayShares(uint256,address).selector;
definition borrowSig() returns uint32 = sig:borrow(uint256,address,address,bool).selector;
definition borrowSharesSig() returns uint32 = sig:borrowShares(uint256,address,address,bool).selector;
definition leverageSig() returns uint32 = sig:leverage(uint256,address,address,bool,bytes).selector;
